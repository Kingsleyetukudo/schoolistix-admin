# Admin app API base. The backend admin routes live under /api/admin.
VITE_API_BASE_URL=http://localhost:7000
# Used by the Vite dev proxy below as a fallback target.
VITE_PROXY_TARGET=http://localhost:7000